# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type User {
  id: ID!
  email: String!
  name: String!
  lastLoginAt: DateTime
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type PaginationInfo {
  page: Int!
  limit: Int!
  total: Int!
  totalPages: Int!
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
}

type ContactChannel {
  id: ID!
  type: ContactType!
  value: String!
  createdAt: DateTime!
  updatedAt: DateTime!
}

enum ContactType {
  EMAIL
  PHONE
}

type Contact {
  id: ID!
  name: String!
  contactChannels: [ContactChannel!]!
  createdAt: DateTime!
  updatedAt: DateTime!
}

type Campaign {
  id: ID!
  title: String!
  description: String!
  channelType: CampaignChannel!
  status: CampaignStatus!
  contacts: [Contact!]!
  createdAt: DateTime!
  updatedAt: DateTime!
}

enum CampaignChannel {
  EMAIL
  SMS
  WHATSAPP
  SLACK
  DISCORD
}

enum CampaignStatus {
  DRAFT
  QUEUED
  ACTIVE
  COMPLETED
  FAILED
}

type CampaignsResponse {
  campaigns: [Campaign!]!
  pagination: PaginationInfo!
}

type Query {
  campaign(id: String!): Campaign!
  campaigns(page: Int = 1, limit: Int = 20, filters: CampaignFiltersInput): CampaignsResponse!
  currentUser: User
}

input CampaignFiltersInput {
  search: String
  status: CampaignStatus
  createdAfter: String
  createdBefore: String
  sortBy: CampaignSortBy = CREATED_AT
  sortOrder: SortOrder = DESC
}

enum CampaignSortBy {
  CREATED_AT
  NAME
  STATUS
  UPDATED_AT
}

enum SortOrder {
  ASC
  DESC
}

type Mutation {
  createContact(input: CreateContactInput!): Contact!
  createCampaign(input: CreateCampaignInput!): Campaign!
  addContactsToCampaign(input: AddContactsToCampaignInput!): Campaign!
}

input CreateContactInput {
  name: String!
  contactChannels: [CreateContactChannelInput!]!
}

input CreateContactChannelInput {
  type: ContactType!
  value: String!
}

input CreateCampaignInput {
  title: String!
  description: String!
  channelType: CampaignChannel!
  status: CampaignStatus
  contactIds: [String!]
}

input AddContactsToCampaignInput {
  campaignId: String!
  contactIds: [String!]!
}